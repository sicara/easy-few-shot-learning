version: 2.1
jobs:
  install:
    parameters:
      python-version:
        type: string
    working_directory: ~/project
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - run:
          name: Set python << parameters.python-version >>
          command: |
            pyenv versions
            pyenv global << parameters.python-version >>
            python -m pip install --upgrade pip setuptools wheel
      - checkout
      # Download and cache dependencies
      - restore_cache:
          keys:
            - machine-<< parameters.python-version >>-{{ checksum "setup.py" }}-{{ checksum "dev_requirements.txt" }}
      - run:
          name: Install dependencies
          command: |
            python -m venv venv
            . venv/bin/activate
            pip install --upgrade pip
            pip install -r dev_requirements.txt
      - save_cache:
          paths:
            - ./venv
          key: machine-<< parameters.python-version >>-{{ checksum "setup.py" }}-{{ checksum "dev_requirements.txt" }}
  lint:
    working_directory: ~/project
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - run:
          name: Set python 3.7
          command: |
            pyenv global 3.7.0
            python -m pip install --upgrade pip setuptools wheel
      - checkout
      - restore_cache:
          key: machine-3.7-{{ checksum "setup.py" }}-{{ checksum "dev_requirements.txt" }}
      - run:
          name: run pylint
          command: |
            . venv/bin/activate
            pylint easyfsl
      - store_artifacts:
          path: lint-project-reports
          destination: lint-project-reports

workflows:
  main:
    jobs:
      - install:
          matrix:
            parameters:
              python-version: ["3.6.5", "3.7.0", "3.8.5"]
      - lint:
          requires:
            - install
